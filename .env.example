# ============================================
# Nextflow ERP Backend - Desenvolvimento Local
# ============================================
# Copie este arquivo para .env e configure os valores para desenvolvimento local

# Ambiente .NET
ASPNETCORE_ENVIRONMENT=Development

# JWT Settings
# Gere uma chave segura: openssl rand -base64 32
JWT_KEY=CHANGE_ME_JWT_KEY_MIN_32_CHARS

# Frontend URL (para CORS e referências)
FRONTEND_URL=http://localhost

# API URL (usado internamente)
BACKEND_URL=http://localhost:8080

# Connection String (para desenvolvimento local com Docker)
# ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=nextflow_db;Username=nextflow_user;Password=dev_password;
